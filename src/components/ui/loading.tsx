const Loading = () => {
  return (
    <div className="w-full min-h-screen bg-black flex items-center justify-center">
      <h2 className="text-2xl loading-wipe font-roboto-mono">Loading</h2>
    </div>
  );
};

export default Loading;
